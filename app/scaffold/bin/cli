#!/usr/local/bin/node
var expressway = require('expressway');

global.EXPRESSWAY_CONTEXT = CXT_CLI;

var app = require('../index');


/**
 * Just a test command.
 * @usage ./bin/cli hello
 */
app.cli.command('hello', "A test command").action((env,opts) =>
{
    app.cli.output(['Hello'], true);
});


app.cli.run();